= simple_form_for [:admin, @event], :html => { :class => "form-horizontal" } do |f|
  = render 'admin/shared/error_messages', model: @event
  = f.input :name, hint: "※30文字以内"
  = f.input :opened_at, hint: "※30文字以内"
  = f.input :closed_at, hint: "※30文字以内"
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    '
    = link_to t('.cancel', :default => t("helpers.links.cancel")), admin_events_path, :class => 'btn'
